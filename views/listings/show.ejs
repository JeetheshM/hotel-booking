<% layout("/layout/boilerplate") %>
<body>
    

    <div class="row mt-3">
        <div class="col-8 offset-3 mt-3">
          <h2>lising details</h2>
        </div>
    <div class="card col-6 offset-3 show-card">
                 
      <img 
  src="<%= listing.image?.url || 'https://images.unsplash.com/photo-1500530855697-b586d89ba3ee' %>"
  class="img-fluid rounded"
  style="max-height: 400px; object-fit: cover;"
  alt="listing_image"
>




        <div class="card-body">
          <p class="card-text mt-3"><b><%= listing.title %></b>
            <br>
          Description: <%= listing.description %><br>
          Price:
                        <% if (listing.price != null) { %>
                          &#8377;<%= listing.price.toLocaleString("en-IN") %> /night
                        <% } else { %>
                          Not specified
                        <% } %>
                        <br>
            Location: <%= listing.location %><bR>
            Country: <%= listing.country %>

        </div>
    </div>
    <!-- <ul> -->
        <!-- <li>Title: <%= listing.title %></li> -->
        <!-- <img src="<%= listing.image %>" alt="Listing Image" style="max-width:300px;"> -->
        <!-- <li> -->
  <!-- Price: -->
  <!-- <% if (listing.price != null) { %>
    &#8377;<%= listing.price.toLocaleString("en-IN") %> per night
  <% } else { %>
    Not specified
  <% } %> -->
<!-- </li>
        <li></li>
        <li></li>
        <li></li>
    </ul> -->

    <div class="btns">
       <a href="/listings/<%=listing.id_%>/edit" class="btn btn-dark add-btn col-1 offset-3">Edit </a>
    
     
          <form method="post" action="/listings/<%= listing._id%>?_method=DELETE" >
          <button class="btn btn-dark  offset-5">Delete </button>
        </form>
    </div>
    

   <!-- <button><a href="/listings">back to listings</a></button> -->
</body>

</div>
